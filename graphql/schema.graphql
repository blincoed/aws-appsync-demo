type Cause {
  id: Int!
}

input CreateProjectInput {
  name: String!
}

type Query {
  getProjects(causeId: String!): Cause
}

type Mutation {
  createProject(input: CreateProjectInput): Cause
}

schema {
  query: Query
  mutation: Mutation
}
